{
  "name": "estudos-concursos",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && cp build/dashboard.js dashboard.js && cp build/auth.js auth.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && python3 -m unittest discover -s tests -p '*_test.py'",
    "supabase:deploy": "npx supabase functions deploy dashboard && npx supabase functions deploy session && npx supabase functions deploy tec-sync && npx supabase functions deploy events && npx supabase functions deploy plan"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.0",
    "vite": "^7.1.5"
  },
  "devDependencies": {
    "supabase": "^2.117.0",
    "typescript": "^7.0.2"
  }
}
